## 结构识别题

### 1. 模块职责归属

假设你有一段Jupyter笔记本代码，包含以下功能：
- 从CSV文件读取订单数据
- 计算月度销售额和客户复购率
- 使用折线图展示销售额趋势
- 使用饼图展示客户地区分布

请将上述功能分配到正确的模块中：
- data_loader.py
- metrics.py
- dashboard.py

### 2. 工具链执行顺序

阅读以下工作流描述，请按正确顺序排列所需的工具调用步骤：

A. 执行 terminal 命令安装依赖  
B. 使用 Write 工具创建 metrics.py  
C. 使用 Read notebook 工具读取原始笔记本  
D. 撰写重构提示词给Claude  
E. 创建 requirements.txt 文件

---

## 机制理解题

### 3. @符号引用语法的作用

在Claude Code中使用@符号引用文件或文件夹时，主要解决什么问题？请结合笔记本重构场景举例说明。

### 4. 迭代式优化的适用时机

课程中提到“急于求成”是易错点。请解释：
- 为什么不应该在单次提示词中要求所有功能？
- “迭代式方法”在本课程场景中具体指哪几步？

---

## 最小实现题

### 5. 基础data_loader.py实现

假设存在一个 `sales.csv` 文件，包含 `date`, `product`, `amount` 三列。请编写一个最小的 `data_loader.py`，包含：
- 一个函数加载CSV数据
- 一个函数按年份筛选数据
- 类型提示（type hints）

### 6. 最小Streamlit仪表盘

使用上题的数据加载器，编写一个最小的Streamlit仪表盘，包含：
- 页面标题
- 显示数据的基本表格
- 至少一个KPI指标展示

---

## 最小替代练习

### 7. JupyterLab原生重构路径

如果你的环境中没有Claude Code，请设计一个用JupyterLab原生功能实现同等重构效果的工作流程：
- 哪些JupyterLab插件可以辅助模块拆分？
- 如何在JupyterLab中管理多个.py文件与.ipynb文件的联动？
