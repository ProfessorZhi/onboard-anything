# P7 练习题：GitHub集成与Hooks探索

## 结构识别题

**题目1：GitHub集成完整流程**

请根据本P内容，还原Claude Code与GitHub集成的完整流程图，标注：
- 每一步需要的外部组件（App / Actions / CLI）
- 各步骤之间的依赖关系
- 失败点排查的关键检查项

---

**题目2：Hooks事件触发路径**

描述一个PR审查场景中，以下两个Hook的触发顺序和作用：
- on_tool_use
- on_task_stop

请说明它们分别在代码审查生命周期的哪个阶段被调用，以及各自的典型用途。

---

## 机制理解题

**题目3：Hook matcher规则设计**

Claude Code的Hook matcher支持精确匹配和模式匹配两种方式。分析以下场景：
- 需要在所有shell命令执行前拦截
- 需要在特定工具（如Bash）执行前拦截
- 需要在用户发送包含"deploy"关键词的消息时触发

请分别给出matcher配置片段。

---

**题目4：Issue驱动开发的闭环流程**

在Issue驱动开发模式下，Claude被@提及后的完整处理流程是什么？请从事件触发开始，追踪到PR合并前的所有关键节点，并指出每个节点涉及的GitHub资源和Claude Code能力。

---

## 最小实现题

**题目5：本地Hooks配置**

在项目中创建`.claude/settings.json`，实现：
- 任何Bash工具执行前，输出"即将执行命令：[命令]"
- 任务结束时，朗读"Task completed"

请给出完整的settings.json配置。

---

**题目6：PR审查Actions配置**

设计一个最小化的GitHub Actions工作流，实现：
- 触发条件：PR创建或更新
- 步骤：让Claude Code分析PR差异并输出审查报告
- 输出：评论到PR中

请给出完整的YAML配置文件结构。

---

## 最小替代练习

**题目7：本地Git Hooks替代练习**

由于Claude Code的Hooks是项目级配置，本练习用标准Git Hooks作为替代：

在本地Git仓库中配置`pre-commit` hook，实现：
- 在每次提交前，检查暂存区中是否包含console.log调试语句
- 如果包含，拒绝提交并输出警告信息

提示：标准Git Hooks位于`.git/hooks/`目录，与Claude Code Hooks虽然机制不同，但都是"在特定事件前后注入自定义逻辑"的思路。
