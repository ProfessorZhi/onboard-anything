---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 15-【基础篇】数据类型总结
page_index: 15
page_title: 【基础篇】数据类型总结
type: 练习题
tags:
  - 网课
  - 练习题
---

# P15 练习题 - 【基础篇】数据类型总结
## 结构识别

**题目 1.1**
Python 的十种数据类型，按"处理数据量"可划分为哪两大类？请将十种类型分别归入对应类别。

---

**题目 1.2**
以下代码的执行结果是什么？是否报错？

```python
s = "Hello"
s[0] = "h"
print(s)
```

---

**题目 1.3**
判断正误并说明理由：对于字典 `d = {"a": 1, "b": 2}`，表达式 `"a" in d` 和 `"a" in d.values()` 的判断含义有何不同？

---

## 机制理解

**题目 2.1**
Python 中关于 bool 转换有句话叫"非零非空即为 True"。请判断以下表达式的值，并指出哪个结果违背直觉：

```python
bool("")
bool(" ")
bool(0)
bool(0.0)
bool([])
bool([0])
```

---

**题目 2.2**
`int(1.9)` 的结果是多少？与 `round(1.9)` 有何区别？请解释背后的机制。

---

**题目 2.3**
`sum()`、`max()`、`min()` 三个聚合函数都能作用于哪些数据类型？请说明它们的限制。

---

**题目 2.4**
以下代码为何不安全？请说明 `eval()` 的使用边界。

```python
user_input = input("输入任意表达式：")
result = eval(user_input)
print(result)
```

---

## 最小实践

**题目 3.1**
完成以下类型转换（若报错则写"报错"）：

| 原始值 | `int()` | `float()` | `bool()` | `str()` |
|--------|---------|-----------|----------|---------|
| `"3.14"` | ① | ② | ③ | "3.14" |
| `"abc"` | ④ | ⑤ | ⑥ | "abc" |
| `3.7` | ⑦ | 3.7 | ⑧ | ⑨ |

---

**题目 3.2**
已知列表 `nums = [3, 1, 4, 1, 5, 9, 2, 6]`，请用一行代码实现：
1. 将其转换为集合去重
2. 求去重后集合的最大值
3. 判断数字 `5` 是否在集合中

---

**题目 3.3**
将字符串 `"key1:value1,key2:value2"` 转换为字典，要求写出完整代码并说明思路。

---

## 扩展思考

**题目 4.1**
某系统需要存储用户的选择（可多选，且顺序无关），应该用 `list` 还是 `set`？请从操作需求和数据特性两个角度分析。如果需要知道用户的选择顺序，应该如何调整方案？

---

**题目 4.2**
`dict()` 构造函数要求提供"键值对结构"才能成功转换。请思考：为什么 Python 不允许 `dict([1, 2, 3])` 这样的写法？尝试给出两种能成功转换为字典的可迭代对象结构，并总结其规律。

---

## 最小替代练习

**题目 5.1**（PyCharm 最小替代）
本 P 使用 PyCharm 进行代码演示。如果你在没有安装 PyCharm 的环境下练习，可以使用以下替代方案：

| 方案 | 说明 |
|------|------|
| VS Code + Python 插件 | 轻量级 IDE，免费开源 |
| PyCharm Community | JetBrains 出品，社区版免费 |
| Jupyter Notebook | 交互式编程，适合逐行调试 |
| 在线环境 | 如 replit.com、python.org/shell |

**任务**：选择其中任意一个方案，配置好 Python 运行环境，验证以下三行代码的执行效果：

```python
print(type(3.14))
print(isinstance("hello", str))
print(len("你好"))
```

记录你的环境配置过程（截图或文字描述），这是后续所有练习的基础环境验证。
