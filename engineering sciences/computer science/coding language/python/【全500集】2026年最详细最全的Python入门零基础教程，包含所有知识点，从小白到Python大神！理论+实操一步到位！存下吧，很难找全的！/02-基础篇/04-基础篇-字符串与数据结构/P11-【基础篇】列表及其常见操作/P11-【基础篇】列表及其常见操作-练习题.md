---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 11-【基础篇】列表及其常见操作
page_index: 11
page_title: 【基础篇】列表及其常见操作
type: 练习题
tags:
  - 网课
  - 练习题
---

# P11 练习题 - 【基础篇】列表及其常见操作
---

## 结构识别

**1. 索引定位**
已知 `fruits = ['apple', 'banana', 'cherry', 'date', 'elderberry']`，执行以下代码，分别写出 A、B、C 的值：

```python
fruits = ['apple', 'banana', 'cherry', 'date', 'elderberry']
A = fruits[0]
B = fruits[-2]
C = fruits[2:4]
```

---

**2. 嵌套访问**
已知 `matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]`，执行以下代码，分别写出 D、E 的值：

```python
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
D = matrix[0][1]
E = matrix[2][0]
```

---

**3. 切片逆序**
已知 `nums = [0, 1, 2, 3, 4, 5]`，执行以下代码，写出 F 的值：

```python
nums = [0, 1, 2, 3, 4, 5]
F = nums[::-1]
```

---

## 机制理解

**4. 可变性的核心区别**
列表与字符串的根本区别是什么？请结合"可修改"这一特性举例说明。

---

**5. 方法返回值行为差异**
`count()` 和 `index()` 在处理"元素不存在"时，返回值有何不同？哪种会触发异常？

| 方法 | 元素不存在时的行为 |
|------|-------------------|
| `list.count(x)` | ？ |
| `list.index(x)` | ？ |

---

**6. 原地修改方法**
以下方法的返回值均为 `None`，请说明这意味着什么，并列举至少三个返回值是 `None` 的列表方法。

---

## 最小实践

**7. 添加操作**
编写代码实现：
- 创建空列表 `lst = []`
- 依次 `append('a')`、`append('b')`
- 使用 `insert()` 在索引 0 位置插入 `'c'`
- 使用 `extend()` 添加字符串 `'de'`

最终打印列表，验证结果。

---

**8. 删除与查找操作**
编写代码实现：
- 列表 `nums = [3, 1, 4, 1, 5, 9, 2, 6]`
- 使用 `remove()` 删除第一个 `1`
- 使用 `index()` 找到 `5` 的位置并打印
- 使用 `count()` 统计 `1` 出现的次数并打印

---

**9. 排序与类型验证**
编写代码实现：
- 列表 `words = ['banana', 'apple', 'cherry']`
- 对列表进行原地排序
- 使用 `type()` 验证排序后列表的类型
- 使用 `reverse()` 反转列表并打印

---

## 扩展思考

**10. extend() 与可迭代协议**
`extend()` 只能接受可迭代对象。执行以下代码，分别写出 A、B、C 的结果，并解释原因：

```python
# 代码 A
lst1 = [1, 2, 3]
lst1.extend([4, 5])
print(lst1)

# 代码 B
lst2 = [1, 2, 3]
lst2.extend("abc")
print(lst2)

# 代码 C - 哪一行会报错？
lst3 = [1, 2, 3]
lst3.extend(6)   # 行1
lst3.extend([6]) # 行2
```

---

**11. 列表方法综合对比**
下表对比了三种添加方法和三种删除方法。请查阅文档或实践后，填写空缺处：

| 操作类型 | 方法 | 添加/删除位置 | 单个/多个 | 接受参数类型 |
|---------|------|--------------|----------|-------------|
| 添加 | `append(x)` | 尾部 | 单个 | 任意类型 |
| 添加 | `extend(x)` | ？ | 多个 | ？ |
| 添加 | `insert(i, x)` | 指定位置 | 单个 | 任意类型 |
| 删除 | `remove(x)` | 首个匹配 | 单个 | ？ |
| 删除 | `pop()` | 尾部 | 单个 | 无参数 |
| 删除 | `pop(i)` | 指定位置 | 单个 | 索引值 |

---

## 参考答案框架

| 题号 | 考察点 | 关键答案要点 |
|------|--------|-------------|
| 1 | 索引与切片 | A='apple', B='date', C=['cherry', 'date'] |
| 2 | 嵌套访问 | D=2, E=7 |
| 3 | 切片步长 | F=[5, 4, 3, 2, 1, 0] |
| 4 | 可变性 | 列表可直接修改元素，字符串不可变（需重建） |
| 5 | 方法行为 | count()返回0，index()触发ValueError |
| 6 | None返回值 | 原地修改，不返回新列表；append/extend/insert/remove/sort/reverse |
| 7 | 添加操作 | ['c', 'a', 'b', 'd', 'e'] |
| 8 | 删除查找 | [3, 4, 1, 5, 9, 2, 6]，index=4，count=1 |
| 9 | 排序类型 | ['apple', 'banana', 'cherry']，type=<'list'> |
| 10 | 可迭代协议 | A=[1,2,3,4,5]，B=[1,2,3,'a','b','c']，行1报错TypeError |
| 11 | 方法对比 | extend接收可迭代对象，remove/insert接受任意类型 |
