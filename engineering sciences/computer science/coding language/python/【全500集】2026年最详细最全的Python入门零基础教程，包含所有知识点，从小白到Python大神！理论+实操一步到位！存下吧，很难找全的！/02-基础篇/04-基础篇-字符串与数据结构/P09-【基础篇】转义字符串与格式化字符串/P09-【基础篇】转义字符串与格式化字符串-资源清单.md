---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 9-【基础篇】转义字符串与格式化字符串
page_index: 9
page_title: 【基础篇】转义字符串与格式化字符串
type: 资源清单
tags:
  - 网课
  - 资源清单
---

# P09 资源清单 - 【基础篇】转义字符串与格式化字符串
## 一、语法与特性类

### 1.1 转义字符

| 名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---|---|---|---|---|
| `\t`（制表符） | Python转义字符 | 对齐文本，效果等同于Tab键 | 字幕直接提到 | 制作对齐的表格输出、制作课程表或日程表 |
| `\n`（换行符） | Python转义字符 | 让文字另起一行，实现分行输出 | 字幕直接提到 | 生成多行文本文件、打印分行的日志输出 |
| `\\`（双反斜杠） | Python转义字符 | 在字符串中输出单个反斜杠 | 字幕直接提到 | 处理Windows文件路径的转义、输出反斜杠相关的符号 |
| `\'`（转义单引号） | Python转义字符 | 在单引号字符串内输出单引号 | 字幕直接提到 | 输出包含引号的对话文本、处理英文所有格 |
| `\"`（转义双引号） | Python转义字符 | 在双引号字符串内输出双引号 | 字幕直接提到 | 输出JSON格式的字符串、处理包含引号的文本 |
| `\r`（回车符） | Python转义字符 | 光标回到行首（课程未展开） | 基于上下文推断 | 实现覆盖输出的动画效果、文本替换场景 |
| `\b`（退格符） | Python转义字符 | 删除前一个字符（课程未展开） | 基于上下文推断 | 实现打字机效果、制作进度条回退动画 |

---

### 1.2 字符串字面量类型

| 名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---|---|---|---|---|
| 原始字符串 `r"..."` | Python语法特性 | 让反斜杠作为普通字符处理，避免转义疲劳 | 字幕直接提到 | 处理Windows文件路径、处理正则表达式、处理Windows注册表路径 |
| 格式化字符串 `f"..."` | Python语法特性 | 将变量、表达式、函数调用嵌入字符串，实现优雅拼接 | 字幕直接提到 | 生成动态报表、构建SQL语句、生成HTML/JSON片段 |

---

### 1.3 f-string格式化语法

| 名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---|---|---|---|---|
| `{变量}` | f-string变量占位符 | 嵌入变量值 | 字幕直接提到 | 构建个性化问候语、动态拼接用户信息 |
| `{表达式}` | f-string表达式语法 | 在花括号内直接计算算术表达式 | 字幕直接提到 | 计算购物车总价、计算BMI指数 |
| `{函数调用}` | f-string函数语法 | 在花括号内执行函数并嵌入结果 | 字幕直接提到 | 嵌入`len()`、`str.upper()`等函数结果 |
| `{变量:宽度}` | f-string宽度设置 | 指定输出宽度，不足时补空格（数字默认右对齐，字符串默认左对齐） | 字幕直接提到 | 制作对齐的成绩单、制作格式化的账单 |
| `{变量:0宽度}` | f-string补零填充 | 宽度不足时前面补零 | 字幕直接提到 | 格式化订单编号、格式化时间显示（补零场景） |
| `{变量:.nf}` | f-string浮点精度 | 保留n位小数（自动四舍五入，不足时补零） | 字幕直接提到 | 格式化货币金额、格式化百分比显示 |
| `{变量:<宽度}` | f-string左对齐 | 内容左对齐，右侧补空格 | 字幕直接提到 | 制作左对齐的列表、对比不同对齐方式的排版效果 |
| `{变量:>宽度}` | f-string右对齐 | 内容右对齐，左侧补空格 | 字幕直接提到 | 制作右对齐的数字列、制作表格右列 |
| `{变量:^宽度}` | f-string居中对齐 | 内容居中对齐，两侧补空格 | 字幕直接提到 | 制作居中的标题、制作对称的装饰文本 |
| `{变量:填充符^宽度}` | f-string自定义填充 | 居中时使用指定字符填充（只能是单个字符） | 字幕直接提到 | 制作星号边框标题、制作分隔线、创建ASCII艺术框 |

---

## 二、函数与内置对象类

| 名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---|---|---|---|---|
| `type()` | Python内置函数 | 演示f-string中可调用函数，输出变量类型 | 字幕直接提到 | 检查用户输入类型、调试数据类型相关问题 |
| `print()` | Python内置函数 | 配合sep参数对比传统拼接方式与f-string | 基于上下文推断 | 调试格式化输出、验证字符串处理结果 |
| `str`类型 | Python内置类型 | f-string操作的主要对象类型 | 基于上下文推断 | 字符串切片、查找、替换等进阶操作 |
| `int`类型 | Python内置类型 | f-string整数格式化的操作对象 | 基于上下文推断 | 进制转换显示、数值计算结果格式化 |
| `float`类型 | Python内置类型 | f-string浮点精度设置的操作对象 | 基于上下文推断 | 科学计数法显示、货币精度处理 |

---

## 三、工具与环境类

| 名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---|---|---|---|---|
| Windows资源管理器 | 系统工具 | 验证路径转义正确后文件是否可访问 | 字幕直接提到 | 排查路径错误、验证文件操作结果 |
| Python 3.6+ | 运行环境 | f-string语法的版本要求（课程使用） | 字幕直接提到 | 确认Python版本、检查版本兼容性问题 |
| VS Code / 任意IDE | 代码编辑器 | 编写和运行Python代码 | 基于上下文推断 | 配置Python开发环境、安装代码补全插件 |
| CONSOLAS字体 | 等宽字体 | 调试对齐显示时保证字符宽度一致 | 字幕直接提到 | 配置终端等宽字体、处理ASCII艺术对齐 |

---

## 四、应用场景类

| 名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---|---|---|---|---|
| Windows文件路径 | 应用场景 | 演示反斜杠转义问题，如 `D:\tools\download_tools\Python` | 字幕直接提到 | 编写文件批量处理脚本、处理用户选择的文件路径 |
| 九九乘法表 | 后续应用预告 | 预告 `\t` + 循环 + f-string 的组合应用 | 字幕直接提到 | 实现九九乘法表输出、制作其他对齐表格 |
| 正则表达式 | 关联应用场景 | 原始字符串适合处理含大量反斜杠的正则模式 | 基于上下文推断 | 编写数据验证规则、实现文本提取功能 |
| 动态文本输出 | 应用场景 | f-string拼接变量与文字的典型场景 | 基于上下文推断 | 生成个性化邮件模板、构建日志消息 |

---

## 五、延伸学习资源类

| 名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---|---|---|---|---|
| Python官方文档：格式化字符串字面量 | 官方文档 | f-string语法完整参考 | 基于上下文推断 | 查阅完整的格式化规格说明 |
| Python官方文档：字符串方法 | 官方文档 | 学习`strip()`、`replace()`、`split()`等字符串方法 | 基于上下文推断 | 字符串清洗、分割、查找等进阶操作 |
| `%` 格式化（旧式） | 历史语法 | Python 3.6之前的主流格式化方法（作为对比了解） | 基于上下文推断 | 阅读旧代码时能理解历史遗留代码 |
| `.format()` 方法 | 旧式格式化 | Python 2.6引入的格式化方法（作为f-string前置了解） | 基于上下文推断 | 对比学习不同格式化方法的优劣 |

---

## 六、资源清单速查表

| 类别 | 资源数量 | 代表资源 |
|---|---|---|
| 转义字符 | 7个 | `\n`、`\t`、`\\`、`\'`、`\"` |
| 字符串语法 | 2种 | `r"..."`（原始字符串）、`f"..."`（格式化字符串） |
| f-string格式化语法 | 10种 | `{变量:宽度}`、`{.nf}`、对齐符号 |
| 内置函数/类型 | 5个 | `type()`、`print()`、`str`、`int`、`float` |
| 工具与环境 | 4个 | Windows资源管理器、Python 3.6+、IDE、CONSOLAS字体 |
| 应用场景 | 4个 | 文件路径、九九乘法表、正则表达式、动态文本输出 |
| 延伸学习资源 | 4个 | 官方文档、`%`格式化、`.format()`方法 |

---

**备注**：本清单涵盖本P直接讲解及在讲解过程中自然涉及的资源。课程未明确提及但可合理推断的延伸资源（如`\r`、`\b`等转义字符，`%`格式化方法）已标注来源为“基于上下文推断”，供学习者参考。
