# T01 答案 - P06 输入函数进阶测试

## 使用方式

- 你直接在聊天里把答案发给我
- 我负责把你的答案整理到这份文件
- 这份文件保留整理后的答案、原始思考过程摘要和疑惑点
- 详细批改写入 `讲评.md`

## 当前状态

- 提交状态：已提交第 1 轮
- 提交方式：聊天发送
- 最近整理时间：2026-04-21

## 答案记录

## 第 1 轮提交

### 1.

- 提炼答案：
  - `raw` 是 `str`，值是 `"10 20"`
  - `parts` 是 `list`，内容是 `["10", "20"]`
  - `nums` 是 `map` 对象，展开后会产出两个 `int`：`10` 和 `20`
  - `a`、`b` 都是 `int`，分别是 `10`、`20`
- 原始思考摘要：
  - 你能看清 `raw -> parts -> map -> a,b` 这条链
  - 对“`a, b = nums` 为什么能成立”还带着试探口吻
- 疑惑点：
  - `map` 对象能否被这样直接解包
  - `map` 和“自动迭代器”的关系是什么

### 2.

- 提炼答案：
  - 写法 A 不报错，`a`、`b` 是字符串 `"3"`、`"5"`，`print(a + b)` 输出 `"35"`
  - 写法 B 不报错，`a`、`b` 是整数 `3`、`5`，`print(a + b)` 输出 `8`
- 原始思考摘要：
  - 你基本抓到了 A 是字符串拼接、B 是整数相加
  - 你把左侧多变量接收和“字典”混在一起了
- 疑惑点：
  - 解包赋值是不是语法糖
  - 左边多个变量时，右边到底需要什么类型
  - 为什么不是“字典赋值”

### 3.

- 提炼答案：
  - `type(a)` 是 `str`
  - `type(b)` 是 `list`
  - `type(c)` 是 `map`
  - 这三者里没有一个能“直接”拿来做最终算术；如果只谈离数值转换最近的一层，`c` 比 `a`、`b` 更接近数值流，但它本身仍然不是数字
- 原始思考摘要：
  - 你能正确判断三种类型
  - 你能意识到“`c` 是 map，不等于已经是数值结果”
- 疑惑点：
  - “适合计算”到底是指“已经能算”还是“离可计算更近”

### 4.

- 提炼答案：
  - 会报错
  - 根因是 `"abc"` 无法转换为 `int`
- 原始思考摘要：
  - 你先把注意力放到了 `map` 的构造方式本身
  - 然后回到题目本身，判断出会在字符串转整数时炸掉
- 疑惑点：
  - `map(function, iterable)` 的参数结构还不清楚
  - Python 里“构造函数 / 类型对象 / 实例化”的术语边界还不清楚

### 5.

- 提炼答案：
  - 会报错
  - `split()` 本身没错
  - 错因是右侧给了 3 个元素，而左侧只接 2 个变量，发生解包个数不匹配
- 原始思考摘要：
  - 你能抓到“个数不匹配”
  - 你开始追问“左侧多元素接收”到底在什么条件下成立
- 疑惑点：
  - 解包赋值右侧能是哪些对象
  - `map` / `list` / `dict` 在解包时各自提供的是什么

### 6.

- 提炼答案：
  - `int("18")`：成功
  - `int("18.0")`：报错
  - `float("18")`：成功
  - `float("18.0")`：成功
  - `int("-3")`：成功
  - `float("-3.5")`：成功
- 原始思考摘要：
  - 你整体判断基本正确
  - 对“为什么 `int("18.0")` 不行”还没有展开机制解释
- 疑惑点：
  - 想单独有一份笔记解释常见对象如 `int`、`map` 的构造方式和签名

### 7.

- 提炼答案：
  - `input()`：合法
  - `input("请输入姓名：")`：合法
  - `input(prompt="请输入姓名：")`：不合法
- 原始思考摘要：
  - 你抓住了 `/` 表示只能按位置传参
- 疑惑点：
  - 暂无明显新增疑惑，理解较稳

### 8.

- 提炼答案：
  - `input` 更接近内置函数对象
  - `map`、`str`、`list` 更接近类型对象
  - `map(int, ["1", "2"])` 返回的是 `map` 实例对象
- 原始思考摘要：
  - 你已经能把“函数对象”和“类型对象”分开
  - 还会用“内置对象 / 内置类型”这些更大的笼统词
- 疑惑点：
  - “内置对象”“内置类型”“类型对象”“函数对象”之间层级关系还需要压实

### 9.

- 提炼答案：
  - `f(1, 2, c=3)`：合法
  - `f(1, b=2, c=3, x=9)`：合法，`x` 进 `kwargs`
  - `f(a=1, b=2, c=3)`：不合法，`a` 只能位置传参
  - `f(1, 2, 3, 4, c=5, y=6)`：合法，`args=(3, 4)`，`c=5`，`y` 进 `kwargs`
  - `f(1, 2, d=4)`：不合法，因为缺少必传仅关键字参数 `c`
- 原始思考摘要：
  - 你对这组题整体掌握得不错
- 疑惑点：
  - 暂无明显新增疑惑，理解较稳

### 10.

- 提炼答案：
  - 你想表达的 4 步是：
    1. `raw = input("请输入两个整数：")`
    2. `parts = raw.split()`
    3. `m = map(int, parts)`
    4. `a, b = m`
  - 你后面又给出了更适合初学者的版本：
    - `raw = input(...)`
    - `parts = raw.split()`
    - `x = int(parts[0])`
    - `y = int(parts[1])`
- 原始思考摘要：
  - 你一开始写法受“完整签名 / 类型声明”思路干扰
  - 随后自己纠正回了更 Python 的分步写法
- 疑惑点：
  - `map` 版与 `parts[0]/parts[1]` 版在教学上各自适合什么阶段

### 11.

- 提炼答案：
  - 暂未作答
- 原始思考摘要：
  - 直接说“不会写成一行”
- 疑惑点：
  - 压缩写法与分步写法之间的翻译仍不熟

### 12.

- 提炼答案：
  - 你想写的是一次输入 3 个小数再求平均
  - 规范写法应类似：
    `a, b, c = map(float, input("请输入三个数字：").split())`
    `print((a + b + c) / 3)`
- 原始思考摘要：
  - 你思路是对的
  - 存在全角标点和括号位置问题
- 疑惑点：
  - 表达式优先级与括号位置还不够稳

### 13.

- 提炼答案：
  - 你想先 `split()` 拆成姓名和两个分数，再分别转型并求和
- 原始思考摘要：
  - 主体思路正确
  - 变量命名和符号输入有明显格式问题
- 疑惑点：
  - 从“思路正确”变成“能写成合法 Python 代码”的最后一步还不稳

### 14.

- 提炼答案：
  - 你认为根因是少了 `split()`
  - 这会导致 `map(int, input(...))` 把整行字符串按字符迭代，而不是按空格拆分后的多个字段去转型
- 原始思考摘要：
  - 你抓到了核心问题确实在缺失中间步骤
  - 但“输入 `12 34` 会发生什么”的细节还没展开完整
- 疑惑点：
  - 字符串本身作为可迭代对象参与 `map` 时，到底会按什么粒度迭代

### 15.

- 提炼答案：
  - 你想写一次输入姓名、年龄、身高，再分配给三个变量并转型
  - 当前代码有明显语法问题，但目标流程是对的
- 原始思考摘要：
  - 你知道需要一次 `input()`、拆分、分别转 `int` / `float`
  - 但代码落地时混入了拼写和结构错误
- 疑惑点：
  - `split()` 的调用位置
  - 解包赋值与变量命名的规范写法
  - `print()` 输出中年龄加一的细节

### 16. 额外追问

- 提炼答案：
  - 你当前最想补的不是新题，而是三个机制：
    - 什么是解包赋值
    - `map` 的完整签名是什么
    - 多值赋值为什么成立
- 原始思考摘要：
  - 你已经开始从“会写”转向“为什么这样能工作”
- 疑惑点：
  - 解包赋值底层要求
  - `map` 的类型对象视角
  - “多值赋值”与可迭代对象、解包之间的关系
