---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 28-【基础篇】异常处理
page_index: 28
page_title: 【基础篇】异常处理
type: 练习题
tags:
  - 网课
  - 练习题
---

# P28 练习题 - 【基础篇】异常处理
---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 28-【基础篇】异常处理
page_index: 28
page_title: 【基础篇】异常处理
type: 练习题
tags:
  - 网课
  - 练习题
---

# P28 练习题 - 【基础篇】异常处理

## 结构识别

**Q1：填写各块的职责**

阅读下面的结构，在括号中填入每个块的核心职责（监控 / 捕获 / 无异常分支 / 善后）。

```python
try:          # （　　）
    pass
except ...:   # （　　）
    pass
else:         # （　　）
    pass
finally:      # （　　）
    pass
```

> 参考答案：监控 / 捕获 / 无异常分支 / 善后

---

**Q2：指出下面代码中的两处结构错误**

```python
try:
    result = 10 / 0
except Exception:
    print("出错了")
except ZeroDivisionError:
    print("除零错误")
```

错误一：_______________
错误二（如果把这两个 except 对调，会有什么问题）：_______________

> 提示：参考易错点 6「异常类型覆盖顺序」。

---

## 机制理解

**Q3：判断以下说法是否正确，并简述原因**

| 说法 | 正确/错误 | 原因 |
|------|-----------|------|
| try 块中代码抛出异常后，else 块照常执行 | | |
| finally 块在没有对应 except 的情况下也会执行 | | |
| `except Exception` 可以捕获 SyntaxError | | |
| `except ValueError as e` 中，e 这个名字是固定的，不能修改 | | |

---

**Q4：追踪执行路径**

阅读下面代码，按照实际执行顺序，在空格内写出每行输出（如果该行不会被执行，写"跳过"）。

```python
try:
    x = int("abc")
    print("A")
except ValueError as e:
    print("B:", e)
else:
    print("C")
finally:
    print("D")
print("E")
```

执行顺序输出：___、___、___（共___行输出）

---

## 最小实践

**Q5：实现一个安全的整数输入函数**

要求：
1. 使用 `input()` 获取用户输入
2. 用 `try/except` 捕获 `ValueError`，提示"请输入有效的整数"
3. 用 `else` 块在转换成功时打印"输入合法：{数值}"
4. 用 `finally` 块打印"本次输入结束"（无论成功与否均打印）

在下方写出完整代码，并分别用输入 `"42"` 和 `"hello"` 验证两条路径均正确执行。

```python
# 在此处编写代码
```

**验证清单**
- [ ] 输入 `"hello"` 时程序不崩溃，打印错误提示
- [ ] 输入 `"42"` 时进入 else 块
- [ ] 两种情况下 finally 均执行

---

**Q6：捕获多种异常**

补全下面代码，使其能分别捕获 `ZeroDivisionError` 和 `ValueError`，并对每种异常打印不同的提示信息；捕获范围以「最小化」为原则，不使用裸 `except Exception`。

```python
def safe_divide(a, b):
    try:
        result = int(a) / int(b)
    # 补全 except 块：
    
    
    else:
        print(f"结果：{result}")
```

---

## 扩展思考

**Q7：为什么「捕获所有」是一种坏习惯？**

考虑以下场景：一段网络请求代码用 `except Exception` 兜底，某天服务器地址写错导致 `ConnectionError`，但程序只打印了"出错了"就继续跑。

请回答：
1. 这种写法在生产环境中可能造成什么后果？
2. 更好的做法是什么（至少说出两点改进方向）？

---

**Q8：finally 与 return 的优先级**

预测下面函数的返回值，并解释原因。

```python
def test():
    try:
        return 1
    finally:
        return 2

print(test())
```

输出：___
原因：_______________

> 思考延伸：这个特性在实际编码中应该如何规避？
