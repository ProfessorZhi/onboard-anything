---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 93-【数据分析】概率计算与矩阵
page_index: 93
page_title: 【数据分析】概率计算与矩阵
type: 练习题
tags:
  - 网课
  - 练习题
---

# P93 练习题 - 【数据分析】概率计算与矩阵
## 结构识别

**1.** 请补充完整知识骨架中的空缺环节：

```
均值μ、标准差σ → ____ → 异常值检测
布尔索引 → 数据筛选 → ____
矩阵加减 → 同形要求 → ____
```

**2.** 以下哪个组件不属于"布尔索引可用"的依赖条件？
A. np.array() 转换  
B. 布尔数组（True/False）  
C. Python 列表的 append() 方法  
D. NumPy 数组对象

**3.** 判断正误并说明理由：
- 矩阵乘法要求第一个矩阵的列数等于第二个矩阵的行数
- np.std() 和 np.mean() 可以直接对 Python 列表操作
- 布尔索引返回的是满足条件的原始数据，而不是索引位置

---

## 机制理解

**4.** 某班级平均身高 μ=170cm，标准差 σ=8cm。身高 186cm 的学生在 3σ 准则下是否被判定为异常？请写出计算过程。

**5.** 为什么以下代码会报错？请指出问题并给出修正方案。

```python
heights = [155, 168, 172, 180, 175, 190]
result = heights[heights > 185]
```

**6.** 给定两个 2×2 矩阵：
- A = [[1, 2], [3, 4]]
- B = [[5, 6], [7, 8]]

请计算 A * B（逐元素乘法）和 np.dot(A, B)（矩阵乘法），对比二者在数学含义上的本质区别。

---

## 最小实践

**7.** 完整实现：用 NumPy 实现身高数据的异常值检测（使用 3σ 准则），输出异常值列表。

```python
# 数据
heights = [155, 162, 168, 172, 175, 178, 180, 185, 190, 195, 200, 210]
# 请补全代码
```

**8.** 给定两个矩阵，用代码验证矩阵乘法的维度要求：
- 若维度不匹配，会报什么错误？请写出捕获异常的代码示例。

---

## 扩展思考

**9.** 如果数据集的标准差 σ=0（所有数据完全相同），3σ 准则会失效吗？应该如何处理这种情况？

**10.** 在数据分析中，概率思维除了用于异常值检测，还有哪些实际应用场景？请列举至少两个。

**11.** 如果要处理一个多维数组（3维及以上），布尔索引的机制是否仍然适用？需要考虑哪些维度上的问题？

---

## 最小替代练习

**12.** 纯 Python 实现：不用 NumPy，仅用 Python 内置函数实现标准差的计算（手算公式），并与 np.std() 的结果进行对比。

```python
import numpy as np

data = [72, 75, 68, 70, 73, 71]

# 方法1：NumPy
std_numpy = np.std(data, ddof=0)
print(f"NumPy计算结果: {std_numpy}")

# 方法2：纯Python（请补全）
def manual_std(lst):
    # 请在此实现
    pass

std_manual = manual_std(data)
print(f"纯Python计算结果: {std_manual}")
print(f"二者差值: {abs(std_numpy - std_manual)}")
```

---

## 参考答案概览

| 题号 | 考察点 | 关键答案提示 |
|------|--------|--------------|
| 1 | 知识骨架 | 3σ准则、布尔数组、矩阵乘法 |
| 2 | 依赖关系 | C（列表的append与布尔索引无关） |
| 4 | 计算过程 | \|186-170\|=16, 16>24? 或 16>3×8? |
| 5 | 类型转换 | 必须 np.array(heights) 再做布尔索引 |
| 6 | 乘法区别 | 逐元素 vs 行×列求和 |

> 注：完整代码答案见配套的《代码实战》文档。
