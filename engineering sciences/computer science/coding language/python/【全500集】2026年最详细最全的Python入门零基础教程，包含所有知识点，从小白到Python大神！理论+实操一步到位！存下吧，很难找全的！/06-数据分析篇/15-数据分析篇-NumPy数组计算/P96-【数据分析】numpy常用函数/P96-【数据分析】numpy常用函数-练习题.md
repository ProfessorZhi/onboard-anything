---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 96-【数据分析】numpy常用函数
page_index: 96
page_title: 【数据分析】numpy常用函数
type: 练习题
tags:
  - 网课
  - 练习题
---

# P96 练习题 - 【数据分析】numpy常用函数
---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 96-【数据分析】numpy常用函数
page_index: 96
page_title: 【数据分析】numpy常用函数
type: 练习题
tags:
  - 网课
  - 练习题
---

# P96 练习题 - 【数据分析】numpy常用函数

---

## 一、结构识别

**题1：函数归类**

将下列函数填入对应类别（每格填函数名，可重复使用）：
`np.sum` / `np.argmax` / `np.cumsum` / `np.std` / `np.median` / `np.cumprod` / `np.argmin` / `np.var` / `np.mean` / `np.min`

| 类别 | 函数名 |
|------|--------|
| 基础聚合（单值输出） | |
| 极值定位（返回索引） | |
| 累积过程（输出与输入等长） | |

> 提示：np.min/np.max 属于极值类，但返回的是值不是索引，思考它应归入哪一行。

---

**题2：输出形状判断**

对一个 shape 为 `(3, 4)` 的二维数组 `arr`，判断以下调用的输出形状：

| 调用 | 输出形状 |
|------|----------|
| `np.sum(arr)` | |
| `np.sum(arr, axis=0)` | |
| `np.sum(arr, axis=1)` | |
| `np.cumsum(arr)` | |
| `np.argmax(arr, axis=1)` | |

---

## 二、机制理解

**题3：axis方向辨析**

有如下数组：

```
arr = np.array([[1, 2, 3],
                [4, 5, 6]])
```

不查代码，手动推算：
1. `np.sum(arr, axis=0)` 的结果是什么？说明你的推理过程。
2. `np.sum(arr, axis=1)` 的结果是什么？
3. 为什么说 axis=0 是"列方向"而不是"按列求和"？用一句话描述 axis=0 的实际操作方向。

---

**题4：argmin/argmax与min/max的区别**

对数组 `arr = np.array([3, 1, 4, 1, 5, 9, 2, 6])`：
1. `np.min(arr)` 和 `np.argmin(arr)` 的返回值分别是什么？含义有何不同？
2. 如果数组中有两个相同的最小值（如上例中两个 `1`），`np.argmin` 会返回哪个？这说明什么？
3. 写出一个场景：你只需要 `argmax` 而不需要 `max` 的值。

---

## 三、最小实践

**题5：一维统计全流程**

用以下步骤完成一次完整的统计分析，并记录每一步的输出：

```python
import numpy as np

scores = np.array([88, 72, 95, 60, 83, 91, 78, 65, 87, 70])

# 依次计算并打印：
# 1. 总分
# 2. 平均分
# 3. 中位数
# 4. 标准差（保留2位小数）
# 5. 最高分及其对应的索引位置
# 6. 累积分（cumsum），观察最后一个元素与总分的关系
```

完成后，回答：`cumsum` 的最后一个值和 `sum` 的结果有什么必然关系？为什么？

---

**题6：二维数组的按轴聚合**

创建如下模拟数据并完成任务：

```python
# 模拟3名学生4次考试成绩
grades = np.array([[85, 90, 78, 92],
                   [70, 65, 80, 75],
                   [95, 88, 91, 87]])

# 任务：
# 1. 求每名学生的平均成绩（输出应为3个值）
# 2. 求每次考试的平均成绩（输出应为4个值）
# 3. 找出哪名学生的总分最高（用 argmax 实现，返回索引）
# 4. 找出每次考试的最高分由哪名学生取得（用 argmax + axis，返回索引数组）
```

写出代码并验证输出形状是否符合预期。

---

## 四、扩展思考

**题7：std 与 var 的关系**

1. 在数学上，方差和标准差有什么关系？用一行 NumPy 代码验证：`np.std(arr) ** 2` 与 `np.var(arr)` 是否相等。
2. NumPy 中 `np.std` 默认计算的是**总体标准差**（除以 N），而非**样本标准差**（除以 N-1）。查阅文档，找到切换为样本标准差的参数写法。这个区别在什么场景下会影响结果？

---

**题8：函数选择的判断力**

阅读以下需求，为每条需求选择最合适的 NumPy 函数，并说明理由：

| 需求描述 | 选用函数 | 理由 |
|----------|----------|------|
| 判断一组温度数据的波动大不大 | | |
| 找出销售额最差的那一天是第几天 | | |
| 记录资金账户每日余额的变化过程（每天在前一天基础上累加收支） | | |
| 快速知道一批零件尺寸的中间水平（不受极端值干扰） | | |

---

## 最小替代练习（工具相关）

**题9：Jupyter Notebook 分块验证习惯**

本P推荐在 Jupyter Notebook 中逐函数验证。如果你当前使用的是其他环境（如 VS Code、PyCharm、命令行），完成以下任意一项替代练习：

- **选项A（Jupyter用户）**：将题5的代码拆成6个独立 Cell 分别运行，观察每个 Cell 的输出是否与预期一致。体会分块运行对调试的帮助。
- **选项B（非Jupyter用户）**：在你的环境中，用 `print()` 为题5的每一步输出添加标签（如 `print("总分:", ...)`），使输出可读性等同于 Jupyter 的分块展示效果。
- **选项C（在线环境）**：在 [Google Colab](https://colab.research.google.com) 或 [Kaggle Notebooks](https://www.kaggle.com/code) 免费运行题6的代码，记录你遇到的任何环境差异。

---

## 自查清单

完成以上练习后，确认你能做到：

- [ ] 不查文档，能说出 axis=0 和 axis=1 各自的操作方向
- [ ] 清楚区分 `np.max` 与 `np.argmax` 的返回值类型
- [ ] 知道 `cumsum` 输出的长度规律
- [ ] 能对二维数组正确使用 axis 参数完成按行/列聚合
- [ ] 理解 `std` 默认使用总体标准差，并知道如何切换
