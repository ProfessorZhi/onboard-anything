---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 94-【数据分析】数组的常用操作1
page_index: 94
page_title: 【数据分析】数组的常用操作1
type: 练习题
tags:
  - 网课
  - 练习题
---

# P94 练习题 - 【数据分析】数组的常用操作1
## 结构识别

**题目1**：对于一个 shape 为 (3, 4) 的二维数组，以下关于 axis 的描述，哪一项是正确的？

- A. axis=0 表示沿水平方向（行）操作
- B. axis=1 表示沿垂直方向（列）操作
- C. axis=0 表示沿垂直方向（列）操作
- D. axis 参数对所有 NumPy 操作都没有实际影响

---

## 机制理解

**题目2**：某同学使用 `np.sort(arr, axis=1)` 对一个 3×3 矩阵排序后，发现每一行内部的元素顺序发生了变化，但各行之间的相对顺序保持不变。请解释这个现象，并说明如果改为 `axis=0` 会产生什么不同的效果。

**题目3**：`np.append()` 和 `np.insert()` 的返回值有什么共同特点？这个特点与 NumPy 数组的哪个核心设计原则有关？

---

## 最小实践

**题目4**：给定数组 `arr = np.array([[3, 1, 4], [1, 5, 9]])`，请写出实现以下目标的代码：

- (a) 对每一列分别升序排列
- (b) 将原数组转置
- (c) 在第二列（索引1）的位置插入数值 `7`，沿 axis=0 方向插入

---

## 扩展思考

**题目5**：在 Python 原生循环与 NumPy 矢量化操作之间选择时，通常推荐优先使用矢量化。请思考：如果要计算一个长度为 100 万的一维数组中所有相邻元素的差值，分别用循环和 NumPy 切片实现，两者在代码简洁度和执行效率上可能有哪些差异？这种差异在数组规模扩大到 1 亿时会如何变化？
