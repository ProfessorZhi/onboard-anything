---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 90-【数据分析】numpy的简介和数组的创建
page_index: 90
page_title: 【数据分析】numpy的简介和数组的创建
type: 资源清单
tags:
  - 网课
  - 资源清单
---

# P90 资源清单 - 【数据分析】numpy的简介和数组的创建
## 一、开发与运行环境

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| PyCharm | 开发工具 | 创建数据分析项目、编写和运行NumPy代码 | 字幕直接提到 | 使用PyCharm调试NumPy数组，观察内存占用；配置不同Python解释器管理多版本环境 |
| Python解释器 | 运行时环境 | NumPy依赖的Python执行环境，需与NumPy版本兼容 | 字幕直接提到 | 在不同Python版本（3.8/3.9/3.10+）下测试NumPy行为差异 |

## 二、核心第三方库

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| NumPy 1.19.2 | 第三方库 | 本课核心，用于数值计算、数组操作、线性代数 | 字幕直接提到 | 尝试升级到最新版本，观察新特性；对比不同版本的数组方法差异 |
| numpy.ndarray | 数据结构 | NumPy的核心对象类型，N维数组，所有创建的数组都是此类型 | 字幕直接提到 | 探索ndarray对象的属性和方法；理解不同数据类型（dtype）对存储的影响 |

## 三、标准库与模块

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| time (time模块) | 标准库模块 | 提供time.time()函数用于性能计时 | 字幕直接提到 | 用time模块测量其他操作的性能；对复杂计算进行耗时分析 |

## 四、包管理命令

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| pip list | 包管理命令 | 查看当前环境已安装的所有Python包及版本 | 字幕直接提到 | 定期检查项目依赖；配合虚拟环境管理多个项目的包版本 |
| pip install numpy==1.19.2 | 包管理命令 | 安装指定版本的NumPy，确保与课程环境一致 | 字幕直接提到 | 学习使用pip freeze导出环境依赖；创建requirements.txt管理项目依赖 |

## 五、NumPy核心函数

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| np.array() | NumPy函数 | 将Python列表、元组、range对象转换为NumPy数组 | 字幕直接提到 | 尝试转换不同数据类型；了解dtype参数对数组类型的影响；处理嵌套列表创建多维数组 |
| np.arange() | NumPy函数 | 类似于Python内置range，生成固定步长的数组 | 字幕直接提到 | 用arange生成等差数列；结合reshape创建多维数组；探索与Python range的性能差异 |
| np.linspace() | NumPy函数 | 在指定范围内均匀分布生成指定数量的样本 | 字幕直接提到 | 生成平滑曲线的数据点；对比linspace与arange在数学建模中的不同应用场景 |
| np.sum() | NumPy函数 | 对NumPy数组所有元素求和 | 字幕直接提到 | 对比sum()与np.sum()的性能；使用axis参数对多维数组按轴求和 |

## 六、Python内置对象与函数

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| Python list | 数据结构 | Python内置列表，可作为np.array()的输入 | 字幕直接提到 | 理解列表与NumPy数组的内存布局差异；练习列表推导式与NumPy向量化的对比 |
| Python range | 内置对象 | 生成整数序列，常与np.array()配合使用 | 字幕直接提到 | 探索range对象的惰性求值特性；在大数据场景下对比range与预生成列表的内存占用 |
| Python tuple | 数据结构 | Python内置元组，可作为np.array()的输入 | 字幕直接提到 | 了解元组与列表在NumPy数组创建中的行为差异；探索元组作为函数返回值的场景 |
| sum() | 内置函数 | Python自带的求和函数，逐元素累加 | 字幕直接提到 | 对比内置sum与np.sum的性能；在不同数据规模下测试效率差异 |
| type() | 内置函数 | 返回对象的类型信息，用于验证NumPy数组类型 | 字幕直接提到 | 检查不同创建方式得到的数组类型；验证类型转换结果 |
| list() | 内置函数 | 将可迭代对象转换为列表 | 字幕直接提到 | 对比list()与np.array()的转换差异；理解显式类型转换的必要性 |

## 七、关键参数与配置

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| dtype | 数组属性 | 指定NumPy数组的数据类型，控制元素的存储方式 | 字幕直接提到 | 尝试指定不同dtype（int32/float64/complex128）；理解类型转换对内存和性能的影响 |
| numpy==1.19.2 | 版本号 | 课程指定的NumPy版本，确保方法行为一致 | 字幕直接提到 | 在实际项目中评估是否需要锁定版本；关注版本升级时的Breaking Changes |

## 八、相关文档与资料

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|---------|------|--------------|------|----------------|
| NumPy官方文档 | 文档/网站 | NumPy函数的权威参考资料 | 基于上下文推断 | 作为日常开发的查询手册；学习官方示例代码；关注Release Notes了解版本变化 |
| NumPy数组属性文档 | 文档 | ndarray对象所有属性的说明 | 基于上下文推断 | 深入了解shape、ndim、size、dtype、itemsize等属性的具体含义和用途 |
| NumPy线性代数模块文档 | 文档 | 课程提到的线性代数能力相关文档 | 基于上下文推断 | 预习后续可能涉及的矩阵运算；了解np.linalg模块的功能 |

## 资源依赖关系图

```
开发环境
    │
    └── PyCharm (IDE)
            │
            └── Python解释器
                    │
                    ├── time (标准库)
                    │
                    └── NumPy 1.19.2 (第三方库)
                            │
                            ├── np.array()     ←── Python list / tuple / range
                            ├── np.arange()
                            ├── np.linspace()
                            ├── np.sum()
                            │
                            └── numpy.ndarray (输出类型)
```

## 快速索引

| 关键词 | 关联资源 |
|--------|----------|
| 性能测试 | time.time()、sum()、np.sum() |
| 数组创建 | np.array()、np.arange()、np.linspace() |
| 包管理 | pip list、pip install、numpy==1.19.2 |
| 开发环境 | PyCharm、Python解释器 |
| 类型验证 | type()、dtype |
| 文档查询 | NumPy官方文档 |
