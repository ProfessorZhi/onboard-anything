---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 60-【练习题】11-20
page_index: 60
page_title: 【练习题】11-20
type: 资源清单
tags:
  - 网课
  - 资源清单
---

# P60 资源清单 - 【练习题】11-20
## 资源说明

本P为纯Python基础练习题讲解，全程在本地Python环境中运行，无外部第三方库、在线平台或外部网站依赖。所有涉及的技术资源均为Python内置语法与标准库函数。

---

## 一、Python内置函数

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `input()` | 内置函数 | 获取用户输入（第13-17题均使用） | 字幕直接提到 | 结合文件读写处理批量输入；设计交互式命令行工具 |
| `int()` | 内置函数 | 字符串转整型，用于处理用户输入的整数 | 字幕直接提到 | 进制转换（int(x, base)）；异常处理（try-except包装避免输入非数字崩溃） |
| `float()` | 内置函数 | 字符串转浮点型，第14题用于支持小数比较 | 字幕直接提到 | 财务计算、精度处理（decimal模块）；浮点数陷阱探究 |
| `len()` | 内置函数 | 获取列表/字典长度，配合range()生成下标序列 | 字幕直接提到 | 计算字符串字符数；遍历嵌套结构（矩阵、嵌套列表） |
| `range(start, stop, step)` | 内置函数 | 生成整数序列，第13题用于列表下标遍历，第16题控制循环次数 | 字幕直接提到 | 生成等差数列；替代部分while循环；倒序遍历 |
| `sorted(iterable)` | 内置函数 | 对可迭代对象排序并返回新列表，第14题方式二使用 | 字幕直接提到 | 对字典按键/值排序；降序排列；处理复杂对象排序 |
| `enumerate()` | 内置函数 | 未使用但相关，同时获取索引和值 | 基于上下文推断 | 替代range+len组合；打印带行号的内容；构建索引映射 |

---

## 二、列表操作方法

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `list.insert(i, x)` | 列表方法 | 第13题在指定下标i位置插入元素，保持列表有序 | 字幕直接提到 | 链表模拟实现；批量插入优化；指定位置替换 |
| `list.append(x)` | 列表方法 | 第13题for-else兜底，将元素追加到列表末尾 | 字幕直接提到 | 构建动态数组；队列/栈的push操作；列表拼接 |
| 列表遍历 | 语法模式 | 第11题遍历列表元素求和筛选 | 字幕直接提到 | 列表推导式；多条件筛选；原地修改列表 |

---

## 三、字典操作方法

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `dict.items()` | 字典方法 | 第12题遍历字典键值对，同时获取姓名和年龄 | 字幕直接提到 | 按值排序字典；统计词频；字典推导式 |
| 字典遍历 | 语法模式 | 第12题使用for解包赋值获取key和value | 字幕直接提到 | 嵌套字典遍历；字典的键、值单独遍历 |

---

## 四、字符串操作

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `"str" * n` | 字符串运算 | 第15题构造连续9的数字（"9"*n）；第16题打印星号（"*"*num） | 字幕直接提到 | 图案打印（直角三角形、菱形）；字符画生成 |
| f-string格式化 | 字符串语法 | 多处用于格式化输出（包含变量、表达式） | 字幕直接提到 | 对齐输出；保留小数位数；多进制字符串格式化 |
| `str()` | 内置函数 | 未直接使用但相关，将其他类型转为字符串 | 基于上下文推断 | 拼接输出；文件写入内容准备 |

---

## 五、控制流语法

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `while True` | 循环结构 | 第15、16题构建未知次数的循环（需配合break退出） | 字幕直接提到 | 菜单驱动的程序；等待用户确认；实现重试机制 |
| `break` | 循环控制 | 提前退出循环，第13题插入成功后退出，第15-16题循环结束时退出 | 字幕直接提到 | 多层嵌套循环的指定层退出；模拟开关逻辑 |
| `for-else` | 循环结构 | 第13题for循环正常结束时触发else兜底逻辑（append追加末尾） | 字幕直接提到 | 搜索未找到时的提示；循环完成状态判断 |
| `if-elif-else` | 条件分支 | 第16题判断输入是否在有效范围（1-50）；第17题奇偶判断 | 字幕直接提到 | 多条件分支；边界值处理；菜单选项 |
| 序列赋值 | 语法特性 | 第14题使用`num1, num2 = num2, num1`交换两个变量值 | 字幕直接提到 | 多变量同时赋值；函数返回多值解包 |

---

## 六、特殊常量与初始化技巧

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `float('-inf')` | 内置常量 | 第12题作为最大年龄初始值，确保任何年龄都能替换 | 字幕直接提到 | 求最小值时用`float('inf')`；比较算法初始化 |
| `result = 0` | 初始化模式 | 第11题累加器初始值（0+任何数=任何数） | 字幕直接提到 | 累乘器（result=1）；字符串累积；计数器 |
| `""` 空字符串 | 初始化模式 | 第12题作为姓名占位符 | 字幕直接提到 | 字符串拼接累积；占位符模式 |

---

## 七、数学运算符

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `%` 取模运算符 | 算术运算符 | 第11、15、17题判断整除（余数为0即为整除）；判断奇偶（除2余0为偶） | 字幕直接提到 | 周期性问题（日期循环）；分页计算；倍数判断 |
| `//` 整除运算符 | 算术运算符 | 对比说明（与%区分），强调整除无法区分奇偶 | 字幕直接提到 | 向下取整；位数分离；时间换算 |
| `/` 除法运算符 | 算术运算符 | 第15题计算整除结果（浮点除法输出小数） | 字幕直接提到 | 平均值计算；比例计算；精度控制 |

---

## 八、开发环境

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| Python本地环境 | 运行环境 | 全程在本地.py文件中编写和运行代码 | 字幕直接提到 | 配置IDE（PyCharm/VS Code）；虚拟环境管理；Jupyter Notebook交互式练习 |
| .py文件 | 文件类型 | 保存Python源代码的单位 | 字幕直接提到 | 模块化管理；包结构组织；import机制 |

---

## 九、补充标准库（未直接使用但与本P技能相关）

| 资源名称 | 类型 | 在本P中的作用 | 来源 | 后续可练习方向 |
|----------|------|---------------|------|----------------|
| `random` | 标准库模块 | 未直接使用，可用于生成练习用的随机数据 | 基于上下文推断 | 生成随机列表用于排序练习；模拟数据测试 |
| `time` | 标准库模块 | 未直接使用，可用于性能测量 | 基于上下文推断 | 比较不同排序算法效率；添加延迟效果 |
| `itertools` | 标准库模块 | 未直接使用，可扩展循环技巧 | 基于上下文推断 | 无限迭代器；组合排列生成；循环计数控制 |

---

## 资源汇总表

| 类别 | 数量 | 说明 |
|------|------|------|
| 内置函数 | 7个 | input、int、float、len、range、sorted、enumerate |
| 列表方法 | 2个 | insert、append |
| 字典方法 | 1个 | items |
| 字符串操作 | 2类 | 重复构造、f-string格式化 |
| 控制流语法 | 4种 | while True、break、for-else、if-elif-else |
| 特殊常量 | 3类 | float('-inf')、累加器初始化、空字符串 |
| 数学运算符 | 3个 | %、//、/ |
| 运行环境 | 2类 | Python本地环境、.py文件 |
| 补充标准库 | 3个 | random、time、itertools |

---

## 备注

本P未使用任何外部资源，所有代码均为纯Python内置语法实现。第18-20题内容因字幕截断未收录，可能涉及其他知识点与资源。如需进一步练习，建议将本P涉及的语法点进行组合扩展，例如：用random模块生成随机数据后进行本P涉及的各类操作练习。
