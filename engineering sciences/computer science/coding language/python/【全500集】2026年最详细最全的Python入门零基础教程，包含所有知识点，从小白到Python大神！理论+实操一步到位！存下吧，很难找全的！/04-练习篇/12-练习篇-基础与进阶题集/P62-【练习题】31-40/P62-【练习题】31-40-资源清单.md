---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 62-【练习题】31-40
page_index: 62
page_title: 【练习题】31-40
type: 资源清单
tags:
  - 网课
  - 资源清单
---

# P62 资源清单 - 【练习题】31-40
---

## 说明

本P为纯本地练习题演示，字幕中未提及任何外部网站、文档、仓库或第三方库。所有资源均为Python内置语言特性或本地工具。资源条目的"来源"栏标注依据字幕直接提到还是基于上下文推断。

---

## 一、开发环境

### PyCharm

| 字段 | 内容 |
|---|---|
| **名称** | PyCharm |
| **类型** | 集成开发环境（IDE） |
| **在本P中的作用** | 全部10道练习题的代码编写与运行环境；老师在其中打开预准备.py文件、逐题实现函数并实时运行验证结果 |
| **来源** | 字幕直接提到 |
| **建议方向** | 熟悉PyCharm的运行配置（Run Configuration）；尝试使用内置调试器（Debugger）单步跟踪递归展开过程，直观观察调用栈变化 |

---

### 预准备 .py 文件（每题一个）

| 字段 | 内容 |
|---|---|
| **名称** | 练习题预准备源文件（如 `ex31.py`、`ex34.py` 等） |
| **类型** | Python 源文件（本地文件） |
| **在本P中的作用** | 老师预先写入题目需求描述、示例字典及测试数据，作为每道题的起始模板；学生跟随演示直接在此基础上补充实现代码 |
| **来源** | 字幕直接提到 |
| **建议方向** | 自行创建相同结构的模板文件，养成"先写注释描述三要素，再写实现"的习惯；尝试为每道题补充更多测试用例（边界值、负数、零等） |

---

## 二、Python 内置函数与方法

### dict.values()

| 字段 | 内容 |
|---|---|
| **名称** | `dict.values()` |
| **类型** | Python 内置字典方法 |
| **在本P中的作用** | 练习题31中用于获取字典所有值，返回 `dict_values` 类型对象，随后用 `for` 循环遍历并累加求和 |
| **来源** | 字幕直接提到 |
| **建议方向** | 对比练习 `dict.keys()` 与 `dict.items()`；尝试将 `dict_values` 直接传入 `sum()` 替代手动循环，比较两种写法的可读性与效率 |

---

### range()

| 字段 | 内容 |
|---|---|
| **名称** | `range(start, stop, step)` |
| **类型** | Python 内置函数 / 可迭代对象 |
| **在本P中的作用** | 练习题32中用步长参数（step=2）分别生成偶数序列 `range(2, n+1, 2)` 和奇数序列 `range(1, n+1, 2)`，控制调和级数的项选取 |
| **来源** | 字幕直接提到 |
| **建议方向** | 练习用不同步长生成等差数列；尝试用 `list(range(...))` 打印结果验证索引边界；思考步长为负数时的逆序遍历场景 |

---

### sum()

| 字段 | 内容 |
|---|---|
| **名称** | `sum()` |
| **类型** | Python 内置函数 |
| **在本P中的作用** | 练习题33中配合列表切片使用：`sum(days_in_months[:month-1])` 累加当前月份之前所有月份的天数，替代手动循环累加 |
| **来源** | 字幕直接提到 |
| **建议方向** | 对比手动 `for` 循环累加与 `sum()` 的写法，理解函数式风格的简洁性；尝试为 `sum()` 指定 `start` 参数（第二个参数）并观察效果 |

---

### input()

| 字段 | 内容 |
|---|---|
| **名称** | `input()` |
| **类型** | Python 内置函数 |
| **在本P中的作用** | 练习题33中用于接收用户输入的年份、月份、日期，配合 `int()` 类型转换后传入 `day_of_year()` 函数 |
| **来源** | 字幕直接提到 |
| **建议方向** | 练习对 `input()` 返回值做完整的输入校验（非数字字符串、越界月份日期等），尝试加入 `try/except` 处理类型转换异常 |

---

### int()

| 字段 | 内容 |
|---|---|
| **名称** | `int()` |
| **类型** | Python 内置类型转换函数 |
| **在本P中的作用** | 练习题33中将 `input()` 返回的字符串转换为整数，确保能参与数值运算；老师将此列为易错点 |
| **来源** | 字幕直接提到 |
| **建议方向** | 理解 Python 中类型转换的显式原则；对比 `int()`、`float()`、`str()` 的使用场景；尝试故意省略 `int()` 观察报错信息以加深印象 |

---

### print() / f-string 格式化字符串

| 字段 | 内容 |
|---|---|
| **名称** | `print()` 与 f-string（`f"...{变量}..."`） |
| **类型** | Python 内置函数 / 字符串字面量语法 |
| **在本P中的作用** | 各题中用于输出提示信息和计算结果（如"字典中所有值的总和为"、"某年某月某日是这一年的第X天"）；练习题35中用于格式化输出质因数分解式 |
| **来源** | 字幕直接提到 |
| **建议方向** | 练习 f-string 的多种格式控制符（数字精度、对齐、填充）；尝试用 `str.format()` 改写相同输出，体会两者差异 |

---

## 三、Python 内置语言特性与语法机制

### 列表切片（List Slicing）

| 字段 | 内容 |
|---|---|
| **名称** | 列表切片 `list[start:stop]` |
| **类型** | Python 内置语言特性 |
| **在本P中的作用** | 练习题33中使用 `days_in_months[:month-1]` 截取当前月份之前的所有月份天数列表，配合 `sum()` 完成前置月份天数累加 |
| **来源** | 字幕直接提到 |
| **建议方向** | 系统练习切片的各种形式（负索引、步长、省略start/stop）；重点理解 `[:month-1]` 的"不含当前月"语义，防止常见的差一错误（off-by-one error） |

---

### return None / 提前返回

| 字段 | 内容 |
|---|---|
| **名称** | `return None` / 裸 `return` |
| **类型** | Python 语言特性（函数控制流） |
| **在本P中的作用** | 练习题32、34中用于输入校验失败后提前终止函数执行，避免后续逻辑因非法输入产生错误；老师强调"不写返回值等同于返回None" |
| **来源** | 字幕直接提到 |
| **建议方向** | 理解哨兵值（Guard Clause）模式：将所有非法情况前置处理，使主逻辑保持平坦；对比"if合法 else return"与"if非法 return；主逻辑"两种结构的可读性 |

---

### 递归（Recursion）

| 字段 | 内容 |
|---|---|
| **名称** | 递归函数调用机制 |
| **类型** | Python 语言特性（调用栈机制） |
| **在本P中的作用** | 练习题34中实现斐波那契数列：`return fibonacci(n-1) + fibonacci(n-2)`，老师手动展开递归树讲解终止条件与栈回溯过程，并提示大n值时的性能问题 |
| **来源** | 字幕直接提到 |
| **建议方向** | 用 PyCharm 调试器单步执行观察调用栈深度；尝试用循环迭代版本重写斐波那契函数并对比性能；了解 Python 默认递归深度限制（`sys.setrecursionlimit`）；进阶：了解记忆化（memoization）优化递归重复计算问题 |

---

### 闰年判断逻辑表达式

| 字段 | 内容 |
|---|---|
| **名称** | 闰年判断条件：`(year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)` |
| **类型** | 算法逻辑 / Python 布尔表达式 |
| **在本P中的作用** | 练习题33中封装为辅助函数 `is_leap_year(year)`，返回布尔值；主函数根据返回值动态修改2月天数为29天 |
| **来源** | 字幕直接提到 |
| **建议方向** | 验证各类边界年份（1900、2000、2024、2100）的判断结果；尝试用 Python 标准库 `calendar.isleap()` 进行结果对照验证；思考为何需要 `year % 100 != 0` 这一子条件 |

---

## 四、算法概念（无独立工具，作为知识点资源记录）

### 质因数分解算法

| 字段 | 内容 |
|---|---|
| **名称** | 质因数分解（Prime Factorization） |
| **类型** | 算法概念 |
| **在本P中的作用** | 练习题35的核心算法，函数名为 `prime_factorization(n)`；字幕在参数分析后截断，具体循环除法实现过程不可见，以下内容基于上下文推断 |
| **来源** | 字幕直接提到（函数名与需求描述）；具体实现步骤基于上下文推断 |
| **建议方向** | 自行实现试除法：从2开始，当 `n % i == 0` 时记录因子并持续除以 `i`，直到 `i * i > n`；尝试格式化输出 `90 = 2 × 3 × 3 × 5` 形式；进阶：了解更高效的质因数分解方法（如 Pollard's rho 算法）作为扩展阅读 |

---

## 五、资源总览索引

| 资源名称 | 类型 | 关联练习题 | 来源依据 |
|---|---|---|---|
| PyCharm | IDE | 全部 | 字幕直接提到 |
| 预准备 .py 文件 | 本地源文件 | 全部 | 字幕直接提到 |
| `dict.values()` | 内置字典方法 | 31 | 字幕直接提到 |
| `range(start, stop, step)` | 内置函数 | 32 | 字幕直接提到 |
| `sum()` | 内置函数 | 33 | 字幕直接提到 |
| `input()` | 内置函数 | 33 | 字幕直接提到 |
| `int()` | 内置类型转换 | 33 | 字幕直接提到 |
| `print()` / f-string | 内置函数 / 语法 | 31、33、35 | 字幕直接提到 |
| 列表切片 | 语言特性 | 33 | 字幕直接提到 |
| `return None` / 提前返回 | 语言特性 | 32、34 | 字幕直接提到 |
| 递归调用机制 | 语言特性 | 34 | 字幕直接提到 |
| 闰年判断逻辑表达式 | 算法逻辑 | 33 | 字幕直接提到 |
| 质因数分解算法 | 算法概念 | 35 | 字幕提到需求；实现细节推断 |

---

> **完整性说明**：练习题35的具体实现步骤因字幕截断而不可见，质因数分解算法的实现细节条目中已标注"基于上下文推断"。P62中未出现任何外部文档、网站、第三方库或仓库引用，全部操作在本地PyCharm环境中完成。
