---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 57-【进阶篇】协程基础
page_index: 57
page_title: 【进阶篇】协程基础
type: 练习题
tags:
  - 网课
  - 练习题
---

# P57 练习题 - 【进阶篇】协程基础
## 一、结构识别

**题目 1.1**
协程的底层实现载体是以下哪种Python对象？

- A. 普通函数
- B. 生成器函数（带 yield 的函数）
- C. 线程对象
- D. 进程对象

---

**题目 1.2**
在进程、线程、协程三者中，协程的调度权限归属于谁？

- A. 操作系统
- B. 程序员（代码显式控制）
- C. Python解释器
- D. CPU硬件

---

## 二、机制理解

**题目 2.1**
请说明 `yield` 关键字在协程中承担哪两个核心作用。

---

**题目 2.2**
阅读以下代码，判断执行顺序：

```python
def coro():
    print("A")
    yield
    print("B")
    yield
    print("C")

g = coro()
next(g)
next(g)
```

请问屏幕输出的内容是什么？并说明理由。

---

**题目 2.3**
协程、进程、线程的核心差异是什么？请从**调度方**和**适用场景**两个维度进行对比。

| 维度 | 进程 | 线程 | 协程 |
|------|------|------|------|
| 调度方 | | | |
| 适用场景 | | | |

---

## 三、最小实践

**题目 3.1**
请补全下方代码，实现单线程内的两任务交替执行：

```python
import time

def task(name, next_task):
    """模拟IO密集型任务"""
    for i in range(2):
        print(f"{name} 第 {i+1} 次执行")
        # 此处需要让出执行权
        time.sleep(0.1)

t1 = task("任务A", "任务B")
t2 = task("任务B", "任务A")

# 请写出驱动代码，使两任务交替执行
```

---

**题目 3.2**
以下代码能否正确返回生成器对象？如果调用 `next()` 会发生什么？

```python
def my_coro():
    print("开始执行")
    yield 1
    print("继续执行")
    yield 2

obj = my_coro()
print(type(obj))
# next(obj)
```

请预测注释行执行后的终端输出。

---

## 四、扩展思考

**题目 4.1**
某项目需要同时下载1000个网页内容（IO密集型），现有三个方案：

- 方案A：创建1000个进程
- 方案B：创建1000个线程
- 方案C：使用1000个协程

请分析：
1. 方案A和方案B为何会产生巨大的资源开销？
2. 方案C为何在IO密集型场景下能以极低资源实现高并发？
3. 如果任务变为复杂数学计算（CPU密集型），协程方案还适用吗？为什么？

---

**题目 4.2**
课程提到协程"状态保存"特性。假设你在协程函数中途遇到异常中断，当使用 `next()` 再次唤醒该协程时，恢复的是哪个位置的状态？这与普通函数的执行有何本质区别？
