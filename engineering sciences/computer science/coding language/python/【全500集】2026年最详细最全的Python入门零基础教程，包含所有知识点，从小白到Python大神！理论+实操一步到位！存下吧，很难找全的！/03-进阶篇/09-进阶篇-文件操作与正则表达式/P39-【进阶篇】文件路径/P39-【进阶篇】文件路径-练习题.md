---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 39-【进阶篇】文件路径
page_index: 39
page_title: 【进阶篇】文件路径
type: 练习题
tags:
  - 网课
  - 练习题
---

# P39 练习题 - 【进阶篇】文件路径
## 结构识别

**题目 1-1：路径类型判断**
请判断以下路径属于绝对路径还是相对路径，并说明判断依据。

```
(1) D:\项目\data\config.json
(2) ../images/logo.png
(3) ./output/result.txt
(4) /usr/local/bin/python
(5) reports/2024/summary.pdf
```

---

**题目 1-2：符号含义配对**
请为以下相对路径符号写出其含义：

| 符号 | 含义 |
|------|------|
| . | __ |
| .. | __ |
| ./data | __ |

---

## 机制理解

**题目 2-1：工作目录陷阱**
某项目结构如下，脚本 `run.py` 在 `src` 文件夹中：

```
project/
├── src/
│   └── run.py      ← 脚本位置
├── data/
│   └── input.txt   ← 目标文件
└── config.json
```

小明在 `run.py` 中写 `open("data/input.txt")`，但运行时提示文件不存在。
请分析原因，并说明 `data/input.txt` 的参照点是什么。

---

**题目 2-2：转义问题诊断**
以下代码在 Windows 系统下会出什么错误？原因是什么？

```python
path = "D:\new\folder\file.txt"
print(path)
```

请写出正确写法（至少两种）。

---

**题目 2-3：路径选型判断**
以下场景应该用绝对路径还是相对路径？请说明理由。

- 场景A：读取用户桌面上固定位置的配置文件
- 场景B：读取游戏项目中 `assets/sprites/` 目录下的图片
- 场景C：Python 脚本需要读取 Windows 系统目录下的 hosts 文件

---

## 最小实践

**题目 3-1：路径书写**
假设当前工作目录为 `D:\project`，请写出访问以下文件的路径：

```
D:\project\
├── main.py
├── docs/
│   └── readme.md
└── assets/
    └── images/
        └── logo.png
```

| 文件 | 相对路径写法 |
|------|-------------|
| readme.md | __ |
| logo.png | __ |
| main.py | __ |

---

**题目 3-2：路径验证**
使用 `os` 模块验证当前工作目录：

```python
import os

# 打印当前工作目录
current = ___
print(f"当前工作目录: {current}")

# 判断当前目录下是否存在 "test.txt"
exists = os.path.exists("test.txt")
print(f"test.txt 是否存在: {exists}")
```

请填写空白处的代码。

---

**题目 3-3：改错练习**
以下代码用于读取 `config.ini` 文件（与脚本在同一目录），但报错。请找出所有问题并修正。

```python
path = "C:\Users\Admin\project\config.ini"
f = open(path, "r")
content = f.read()
f.close()
print(content)
```

假设此文件需要移植到其他电脑使用，应该如何改进？

---

## 扩展思考

**题目 4-1：多层返回路径**
某项目结构如下，`module/` 目录下的 `utils.py` 需要读取项目根目录下的 `config.json`，请写出正确的相对路径。

```
my_project/
├── config.json        ← 目标文件
├── data/
│   └── cache.dat
└── module/
    └── utils.py       ← 脚本位置
```

---

**题目 4-2：路径策略对比**
你正在开发一个 Python 工具包 `mylib`，计划开源并供他人安装使用。

- 该工具包需要读取同目录下的 `default_settings.json` 作为默认配置
- 同时需要支持用户指定自定义配置文件路径

请思考：工具包内部读取默认配置时，应该用绝对路径还是相对路径？为什么？
用户自定义配置时，应该如何设计函数接口？

---

## 最小替代练习

**题目 5-1：不用 IDE，手动验证路径**
假设你刚下载了一个 Python 项目，不确定当前终端的工作目录是什么。

请写出一个单行命令，通过 Python 交互式环境快速查看当前工作目录，并验证某文件是否存在。

```bash
python -c "___"
```

---

**题目 5-2：IDE 路径获取的替代方案**
如果你没有 IDE（如 PyCharm）的"复制路径"功能，在 Windows 命令行中，如何获取一个文件的绝对路径？

提示：考虑使用 `cd` 命令和 `for` 循环，或 Windows 自带的 `realpath` 命令（如有）。

---

## 参考答案概览

| 题目 | 考察点 |
|------|--------|
| 1-1 | 区分绝对路径（带盘符或/开头）与相对路径 |
| 1-2 | . 和 .. 的含义 |
| 2-1 | cwd 与脚本所在目录的区别 |
| 2-2 | Windows 反斜杠转义问题 |
| 2-3 | 绝对/相对路径的选型依据 |
| 3-1 | 相对路径书写 |
| 3-2 | os.getcwd()、os.path.exists() 使用 |
| 3-3 | 转义 + 路径硬编码问题 |
| 4-1 | 多级 .. 的使用 |
| 4-2 | 包内路径 vs 用户配置的设计 |
| 5-1 | 命令行 Python 快速验证 |
| 5-2 | Windows 获取绝对路径的方法 |
