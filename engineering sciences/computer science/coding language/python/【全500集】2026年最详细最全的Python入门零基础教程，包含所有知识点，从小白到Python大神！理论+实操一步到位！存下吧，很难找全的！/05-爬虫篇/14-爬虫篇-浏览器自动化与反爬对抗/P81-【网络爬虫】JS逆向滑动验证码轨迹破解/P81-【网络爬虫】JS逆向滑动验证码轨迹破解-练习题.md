---
course: 【全500集】2026年最详细最全的Python入门零基础教程，包含所有知识点，从小白到Python大神！理论+实操一步到位！存下吧，很难找全的！
level: page
chapter: 81-【网络爬虫】JS逆向滑动验证码轨迹破解
page_index: 81
page_title: 【网络爬虫】JS逆向滑动验证码轨迹破解
type: 练习题
tags:
  - 网课
  - 练习题
---

# P81 练习题 - 【网络爬虫】JS逆向滑动验证码轨迹破解
## 1. 结构识别

### 1.1 接口关系题

去哪网登录流程涉及三个接口，请按调用顺序排列以下接口，并说明每个接口的主要作用：

- A. 获取验证码请求
- B. 滑动验证请求
- C. 登录请求

**要求**：写出正确顺序（如 A→B→C）并简述各接口的核心职责。

---

### 1.2 轨迹数据格式题

某滑动验证码的轨迹数据片段如下：

```
[
  ["34567", 0, 38, 0],
  ["34571", 12, 38, 12],
  ["34575", 28, 38, 28]
]
```

请指出每条记录中四个位置分别代表什么含义。

---

## 2. 机制理解

### 2.1 轨迹生成规则理解

某滑动验证码要求总滑动距离为 422px，采用以下策略生成轨迹：

| 步骤 | 参数 | 取值范围 |
|------|------|----------|
| 单次步长 | step | 2~50 px |
| 时间间隔 | interval | 20~35 ms |

**问题**：使用上述策略生成轨迹时，需要多少步才能完成约 422px 的滑动？请说明计算思路，并指出一个潜在风险。

---

### 2.2 AES加密参数识别

在逆向JS代码时，发现以下AES加密配置：

- 加密模式：ECB
- 填充方式：PKCS7
- 密钥长度：16字节（128位）
- 输入数据：JSON序列化后的参数字符串

**问题**：如果要使用 Python 的 PyCryptodome 库复现该加密，需要设置哪些关键参数？请给出核心代码片段中的参数配置。

---

## 3. 最小实践

### 3.1 轨迹构造实践

**任务**：使用 Python 生成一条合法的滑动轨迹数据。

**要求**：
1. 总滑动距离控制在 425px（允许±5px误差）
2. 首条记录必须包含：时间戳后5位、X=0、Y=38（固定）、累计距离=0
3. 每步时间间隔控制在 22~30ms
4. 单次步长控制在 5~40px
5. 最终输出为二维列表格式

**提示**：可以使用 `time.time()` 获取时间戳，`int()` 截取后五位。

---

### 3.2 完整加密参数组装

**任务**：基于以下中间结果，组装最终的验证请求参数。

已知：
- 轨迹数据：`trajectory = [[...], [...], ...]`
- 滑块缺口位置：`缺口X坐标 = 326`
- 背景图片序号：`back_str = "img_1"`

**要求**：按照去哪儿滑动验证码的参数结构，组装出包含以下字段的字典：

| 字段名 | 说明 |
|--------|------|
| captchaType | 固定值 |
| pointJson | 轨迹数据（加密前为字符串） |
| trackData | 轨迹数据（AES加密后） |
| positonJson | 缺口位置数据 |
| backImgStr | 背景图片标识 |

---

## 4. 扩展思考

### 4.1 验证失败溯源

场景：你在测试滑动验证码时，轨迹已构造正确，但多次提交均返回"验证失败"。

**请列举至少3种可能导致验证失败的隐藏原因，并说明定位方向**。

---

### 4.2 轨迹真实性评估

真实的滑动轨迹通常具有以下特征：

- 起步阶段有轻微"试探"动作
- 中段速度相对均匀
- 接近目标时有"顿挫"或减速
- 整体耗时 1.5~3 秒

**问题**：如果你生成的轨迹数据被风控系统标记为"机器轨迹"，可以从哪些维度改进轨迹生成算法？请提出至少2个改进方向。

---

## 5. 最小替代练习

### 5.1 替代网站选择

本P案例网站为去哪儿网（qunar.com）。如果你无法访问该网站进行练习，可以选择以下替代平台进行同类练习：

| 网站 | 验证码类型 | 难度 | 替代可行性 |
|------|-----------|------|-----------|
| 网易云音乐 | 滑动缺口 | 低 | ★★★★☆ |
| B站 | 滑动缺口 | 中 | ★★★★★ |
| 京东 | 点选/滑动 | 高 | ★★★☆☆ |
| 12306 | 滑动缺口 | 中 | ★★★★☆ |

**任务**：选择一个替代网站，使用 Chrome DevTools 抓取其滑动验证码相关接口，尝试分析其轨迹加密方式（注意：仅供学习研究，请遵守网站 robots.txt 和服务条款）。

---

### 5.2 纯Python验证方案

如果无法访问目标网站进行真实请求测试，可以采用以下纯本地练习方案：

**任务**：在无网络请求的情况下，完成以下验证流程：

1. 生成一条模拟轨迹（总距离 425px）
2. 对轨迹数据进行 AES-ECB-PKCS7 加密
3. 构造完整的验证参数字典
4. 将参数字典序列化为 JSON 字符串

**目标输出格式示例**：

```json
{
  "captchaType": "nextSlider",
  "pointJson": "[base64编码或直接字符串]",
  "trackData": "[加密后的字符串]",
  "positionJson": "[缺口位置加密数据]",
  "backImgStr": "img_1"
}
```

**环境依赖**：`pip install pycryptodome`
